<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Optional: For precise midnight updates -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Billing permission -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".DotMatrixApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DotMatrixCalendar">



        <!-- Fluid Calendar Activity (Main) -->
        <activity
            android:name=".ui.fluid.FluidCalendarActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DotMatrixCalendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget Editor Activity -->
        <activity
            android:name=".ui.editor.WidgetEditorActivity"
            android:exported="true"
            android:theme="@style/Theme.DotMatrixCalendar.Editor">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Theme Gallery Activity -->
        <activity
            android:name=".ui.theme.ThemeGalleryActivity"
            android:theme="@style/Theme.DotMatrixCalendar" />

        <!-- Emoji Rules Activity -->
        <activity
            android:name=".ui.emoji.EmojiRuleActivity"
            android:theme="@style/Theme.DotMatrixCalendar" />

        <!-- Pro Upgrade Activity -->
        <activity
            android:name=".ui.pro.ProUpgradeActivity"
            android:theme="@style/Theme.DotMatrixCalendar" />

        <!-- Onboarding Activity -->
        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:theme="@style/Theme.DotMatrixCalendar" />

        <!-- Year View Widget Provider -->
        <receiver
            android:name=".widget.provider.YearViewWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_year_info" />
        </receiver>

        <!-- Month View Widget Provider -->
        <receiver
            android:name=".widget.provider.MonthViewWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_month_info" />
        </receiver>

        <!-- Week View Widget Provider -->
        <receiver
            android:name=".widget.provider.WeekViewWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_week_info" />
        </receiver>

        <!-- Midnight Update Receiver -->
        <receiver
            android:name=".receiver.MidnightUpdateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.DATE_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Chameleon Wallpaper Receiver -->
        <receiver
            android:name=".chameleon.WallpaperChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.WALLPAPER_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
